pub struct UCIEngine {}

impl UCIEngine {
    pub fn new() -> UCIEngine {
        UCIEngine {
            // take in defaults and parameters

            // handle engine-related commands
        }
    }
}
